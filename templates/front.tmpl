<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="utf-8">
    <title>Gestió de DNI's</title>
    <meta name="description" content="Gestió de DNI's en Guanyem Castelló">
    <meta name=viewport content="width=device-width, initial-scale=1">
    <meta name="mobile-web-app-capable" content="yes">

    <!-- build:css styles/vendor.css -->
    <!-- bower:css -->
    <link rel="stylesheet" href="assets/bower_components/semantic/dist/semantic.min.css">
    <!-- endbower -->
    <!-- endbuild -->
    <link href='//fonts.googleapis.com/css?family=Open+Sans:400,700,300&subset=latin,vietnamese' rel='stylesheet' type='text/css'>

    <!-- build:css styles/main.css -->
    <link rel="stylesheet" href="assets/css/main.css">
    <!-- endbuild -->
</head>
<body>
    <div class="ui large menu navbar">
        <div class="left menu">
            <a href="" class="item">Gestió de DNI's</a>
        </div>
        <div class="right menu">
            <a href="http://guanyemcastello.org" class="item">Guanyem Castelló</a>
	    <div class="item">
    	        <a href="?logout" class="ui primary button">Sortir de la sessió</a>
	    </div>
        </div>
    </div>
    <div class="ui segment">
        <h4>Sel.lecciona la fulla de DNI's que vols visualitzar</h4>
        <div class="ui buttons">
            {% for key,p in paginas %}
            <a class="ui big button{% if pagina == key %} active{% endif %}" href="index.php?pagina={{ key }}">
		{% if key == 0 %}
		Sense fulla assignada
                {% else %}
		Fulla {{ key }}
		{% endif %}
	        {% if p > 0 %}
			<div class="ui small teal label" style="margin-left: 1em;">{{ p }} dnis</div>
		{% else %}
			<div class="ui small teal label" style="margin-left: 1em;">cap</div>
		{% endif %}
	    </a>
            {% endfor %}
        </div>
    </div>

    <h2>{% if pagina > 0 %}Fulla {{ pagina }}. {% endif %}Fes click en una imatge per gestionar-la</h2>
    {% if pagina > 0 %}<div class="ui title"><a href="pdf.php?pagina={{ pagina }}"><i class="file pdf outline icon"></i> Impressió de la fulla</a></div>{% endif %}
    <div class="ui segment">
        <div class="ui four column grid">
	    {% if images|length == 0 %}
		<h3 class="ui title">No n'hi han dni's a aquesta fulla.</h3>
	    {% endif %}
            {% for image in images %}
            <div class="column">
                <div class="ui fluid card">
                    <div class="ui image">
                        <a href="crop.php?idni={{ image.idni }}"><img class="ui image" src="image.php?idni={{ image.idni }}"></a>
                    </div>
                    {% if image.ndni %}
                    <div class="content">
			<strong style="font-size: 1.4em;">{{ image.ndni }}</strong>
                    </div>
                    {% endif %}
                    <div class="extra content">
                        <a class="left floated button page" name="{{ image.idni }}" page="{{ image.hoja }}" href="">
                            <i class="block layout icon"></i>
                            Situar a una fulla
                        </a>
                        <a class="right floated button" href="save.php?idni={{ image.idni }}&del=true" onclick="return confirm('Estàs segur/a?');">
                            <i class="delete icon"></i>
                            Esborrar DNI
                        </a>
                    </div>
                </div>
            </div>
            {% endfor %}
        </div>
    </div>

    <h2>Pujada de documents</h2>
    <div class="ui segment">
        <form class="ui form" method="POST" action="save.php" enctype="multipart/form-data">
	    <div class="field">
	        <label>Trïa el arxiu del teu disc dur</label>
	        <input type="file" name="img" />
            </div>
	    <input type="submit" class="ui primary button" value="Pujar document" />
        </form>
    </div>

<div class="ui modal page transition" style="margin-top: -250px;">
    <i class="close icon"></i>
    <div class="header">
      En quina fulla vols ficar aquest DNI?
    </div>
    <div class="content">
        <div class="ui buttons">
            {% for p in paginas %}
            <div class="ui big button set-pagina">
		{% if loop.index0 == 0 %}
		Sense assignar fulla
                {% else %}
		{{ loop.index0 }}
		{% endif %}
	    </div>
            {% endfor %}
            <div class="ui big button set-pagina">{{ paginas|length }}</div>
        </div>
    </div>
    <div class="actions">
      <div class="ui black button cancel">
        Cancel.la
      </div>
    </div>
  </div>

    <script src="assets/bower_components/jquery/dist/jquery.min.js"></script>
    <script src="assets/bower_components/cropper/dist/cropper.min.js"></script>
    <script src="assets/bower_components/semantic/dist/semantic.min.js"></script>
    <script src="assets/js/main.js"></script>

</body>
</html>
